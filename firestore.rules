rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;  // This allows read access to all documents
      allow write: if true;  // This prevents write access
    }
  }
}